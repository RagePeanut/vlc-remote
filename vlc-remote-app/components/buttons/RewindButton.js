import React from 'react';

import ControlButton from './generic/ControlButton';

const RewindButton = () => {
    return (
        <ControlButton
            icon="rewind"
            onPress={() => {}}
        />
    );
};

export default RewindButton;